#include <stdio.h>

int fattoriale(int k)
{
    if(k == 0)
        return 1;
    return k * fattoriale(k - 1);
}

int potenza(int a, int b)
{
    
}

int main()
{
    printf("hello word\n");

    return 0;
}